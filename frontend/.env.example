# Production (Vercel): set to your Railway API URL, no trailing slash
# VITE_API_URL=https://your-api.up.railway.app
#
# Local dev: leave unset — Vite proxies /api and /health to http://127.0.0.1:8001
